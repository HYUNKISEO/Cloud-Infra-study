# 👤 5일차: 리눅스 사용자 및 그룹 계정 관리 (User & Group Management)

> **학습일:** Day 5 <br>
> **학습 목표:** 리눅스의 다중 사용자 시스템 구조를 이해하고, `useradd`, `passwd`, `usermod` 등으로 계정과 그룹을 생성·수정하며, `sudo` 및 `visudo`를 통해 안전하게 관리자 권한을 부여하는 실무 기술을 익힙니다.

---

## 1. 계정 및 그룹 구조와 계정 확인법

리눅스는 다중 사용자(Multi-User) 시스템이므로 각 사용자는 독자적인 계정 ID(UID)와 그룹 ID(GID)를 부여받아 권한을 분리합니다.

```bash
# 현재 로그인된 계정 정보 확인
whoami
# 출력 예시: alex

# 현재 계정의 상세 ID 및 속한 그룹 목록 확인
id
# 출력 예시: uid=1001(alex) gid=1001(alex) groups=1001(alex),27(sudo)
```

---

## 2. 사용자 계정 생성 및 관리 (`useradd`, `passwd`, `usermod`, `userdel`)

계정을 생성할 때는 옵션 없이 실행하면 홈 디렉터리나 기본 쉘이 설정되지 않는 경우가 많아 **필수 옵션과 함께 사용**해야 합니다.

```bash
# 1. 새 계정 생성 (-m: 홈 디렉터리 생성, -s: 기본 쉘 지정)
sudo useradd -m -s /bin/bash alex

# 2. 비밀번호 설정 (생성 직후엔 비밀번호가 없어 로그인 불가)
sudo passwd alex

# 3. 계정 이름 변경 (alex -> bob)
sudo usermod -l bob alex

# 4. 계정 삭제 (-r 옵션을 넣어야 홈 디렉터리까지 깔끔하게 삭제됨)
sudo userdel -r alex
```

---

## 3. 그룹 관리 및 관리자 권한 부여 (`groupadd`, `sudo`, `visudo`)

여러 사용자에게 동일한 접근 권한을 일괄 부여할 때는 그룹을 활용하며, 일반 계정에는 `sudo` 권한을 부여하여 관리자 작업을 수행합니다.

### ① 그룹 생성 및 사용자 추가 (`groupadd`, `usermod`)
```bash
# 1. 새 그룹 생성
sudo groupadd devteam

# 2. 기존 사용자(alex)를 기존 그룹(devteam)에 추가
sudo usermod -aG devteam alex
```

> ⚠️ **`-aG` 옵션 주의사항:**
> `-G`만 사용하면 기존에 속해 있던 다른 그룹에서 탈퇴되고 새 그룹만 남게 됩니다. 반드시 **`-a` (append) 옵션을 붙여 `-aG`로 작성**해야 기존 그룹을 유지하면서 추가됩니다.

### ② 관리자 권한 부여 (`sudo` & `visudo`)
```bash
# 1. sudo 그룹에 사용자 추가 (Ubuntu / Debian 계열)
sudo usermod -aG sudo alex

# 2. wheel 그룹에 사용자 추가 (CentOS / RHEL 계열)
sudo usermod -aG wheel alex

# 3. visudo를 통한 세부 권한 설정 (/etc/sudoers 파일 직접 수정)
sudo visudo
# 설정 예시: alex ALL=(ALL:ALL) NOPASSWD: ALL (비밀번호 입력 없이 sudo 허용)
```

---

## 4. 계정 정보 저장 파일 및 동작 원리

리눅스 시스템은 계정 관련 주요 정보를 핵심 텍스트 파일로 관리하며, 신규 계정 생성 시 초기 환경 파일을 복사해 가져옵니다.

### ① 계정 정보 파일 3가지
1. **`/etc/passwd`**: 전체 사용자 계정 목록과 기본 설정 (누구나 읽기 가능)
   * *구조 예시:* `alex:x:1001:1001::/home/alex:/bin/bash`
   * **UID 구분 체계:**
     * `0`: 무조건 **root** 계정 (최고 관리자)
     * `1 ~ 999`: **시스템/데몬 계정** (백그라운드 서비스 실행 전용)
     * `1000 이상`: **일반 사용자 계정**
2. **`/etc/shadow`**: 암호화된 비밀번호와 만료일 저장 (**root만 읽기 가능**)
3. **`/etc/group`**: 시스템의 모든 그룹 이름과 소속 사용자 목록 저장

### ② `/etc/skel` (신규 계정 초기 뼈대 디렉터리)
`useradd -m` 명령어로 홈 디렉터리를 만들 때, `/etc/skel` 폴더 안의 기본 파일들(`.bashrc`, `.profile` 등)이 새 사용자의 홈 디렉터리로 자동 복사됩니다.

### ③ `su` vs `su -` 계정 전환 차이점
* **`su alex`**: alex 계정으로 전환하지만, 이전 계정의 환경변수(경로 등)를 그대로 유지합니다.
* **`su - alex`**: alex 계정으로 전환하면서 **홈 디렉터리와 환경변수까지 완전히 새로 로딩**합니다. (실무 권장)

---

## 5. 실무 계정 관리 & 보안 가이드

실무 서버 운영 시 보안 강화를 위해 준수해야 하는 계정 관리 규칙입니다.

### ① 계정 일시 잠금 및 해제 (`passwd -l / -u`)
퇴사자 발생이나 보안 이슈 시 계정을 삭제하지 않고 즉시 차단을 적용하는 실무 테크닉입니다.
```bash
sudo passwd -l alex   # Lock: 계정 잠금 (로그인 차단)
sudo passwd -u alex   # Unlock: 계정 잠금 해제
```

### ② 비밀번호 만료 주기 설정 (`chage`)
```bash
# 계정의 비밀번호 만료 정보 확인
sudo chage -l alex

# 90일마다 비밀번호 변경을 강제하도록 설정
sudo chage -M 90 alex
```

> 🛑 **보안 절대 경고 (root 직접 로그인 금지):**
> SSH 설정(`/etc/ssh/sshd_config`)에서 `PermitRootLogin no`로 설정하여 root 직접 접속을 차단하세요. 최고 관리자(`root`)로 직접 로그인하여 작업하면 명령 오타 하나로 시스템 전체가 파괴될 수 있으며 작업 책임 소재 파악이 불가능해집니다. 평소에는 일반 계정으로 접속하고 필요한 권한에만 `sudo`를 사용하는 **최소 권한의 원칙**을 준수해야 합니다.

---

## 6. Day 5 주요 명령어 옵션 상세 요약표

| 명령어 | 옵션 | 주요 기능 및 역할 | 실무/시험 주의사항 |
| :--- | :---: | :--- | :--- |
| **`useradd`** | **`-m`** | 사용자 홈 디렉터리(`/home/계정명`) 자동 생성 | 생략 시 홈 디렉터리가 생성되지 않음 |
| | **`-s`** | 기본 로그인 쉘 지정 (`/bin/bash` 등) | 생략 시 기본 쉘(`/bin/sh`) 지정 가능성 있음 |
| | **`-g`** | 주 그룹(Primary Group) 지정 | 미지정 시 계정명과 동일한 그룹 생성 |
| | **`-G`** | 보조 그룹(Secondary Group) 지정 | 복수 지정 시 콤마(`,`)로 구분 |
| **`usermod`** | **`-aG`** | 기존 그룹을 유지하면서 보조 그룹 추가 | **`-a` 누락 시 기존 보조 그룹 탈퇴됨** |
| | **`-l`** | 계정 로그인 이름(ID) 변경 | `usermod -l 새이름 기존이름` |
| | **`-s`** | 계정의 로그인 쉘 변경 | `usermod -s /bin/zsh alex` |
| **`userdel`** | **`-r`** | 홈 디렉터리와 메일함까지 완전 삭제 | 옵션 미사용 시 디렉터리가 시스템에 남음 |
| **`passwd`** | **`-l` / `-u`**| 계정 비밀번호 잠금(Lock) / 해제(Unlock) | `passwd -l alex` |
| | **`-e`** | 다음 로그인 시 비밀번호 강제 변경 | 신규 입사자 초기 계정 전달 시 활용 |
