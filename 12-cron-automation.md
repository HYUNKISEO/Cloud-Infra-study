# 12일차: 반복 작업 자동화 스케줄러 (crontab)

> **학습 목표:** 리눅스 환경에서 주기적이고 반복적인 작업(백업, 로그 정리, 스크립트 실행 등)을 자동으로 수행해 주는 `cron` 데몬과 `crontab` 주기 설정 문법을 습득하고, 실무 장애를 예방하는 필수 주의사항을 이해합니다.

---

## 1. cron과 crontab의 개념

* **cron (크론):** 백그라운드에서 특정 시간에 예약된 작업을 자동으로 실행해 주는 **리눅스 데몬(서비스)**입니다.
* **crontab (크론탭):** cron 데몬이 참조하는 **'작업 스케줄표(Cron Table)'**이며, 작업 주기와 실행할 명령어를 정의해 두는 파일입니다.

```text
[ 사용자 작성 ]                  [ 백그라운드 데몬 ]            [ 지정된 시각에 실행 ]
  crontab -e  ------------->       cron 데몬       -------------> 백업 / 로그 정리 /
 (스케줄 등록)                   (매 분마다 시각 체크)              자동 스크립트 구동
```

---

## 2. crontab 주기를 결정하는 5개의 별(`* * * * *`) 문법

crontab의 주기는 맨 앞에 위치한 **5개의 필드(공백으로 구분)**로 설정합니다.

```text
*       *       *       *       *       /home/ubuntu/backup.sh
|       |       |       |       |
|       |       |       |       +-- 요일 (0 ~ 7, 0과 7은 일요일, 1:월, 2:화...)
|       |       |       +---------- 월 (1 ~ 12)
|       |       +------------------ 일 (1 ~ 31)
|       +-------------------------- 시 (0 ~ 23)
+---------------------------------- 분 (0 ~ 59)
```

### 2.1 주요 주기 작성 예시 (실무 자주 쓰는 패턴)

| 주기 표현 | 의미 | 설명 |
| :--- | :--- | :--- |
| `* * * * *` | 매 1분마다 | 매 분 0초가 될 때마다 계속 실행 |
| `*/5 * * * *` | 매 5분마다 | 0분, 5분, 10분, 15분... 간격으로 실행 |
| `0 3 * * *` | 매일 새벽 3시 정각 | DB 자동 백업에 가장 흔히 쓰이는 패턴 |
| `0 9 * * 1` | 매주 월요일 오전 9시 | 주간 리포트 생성 또는 정기 점검 |
| `30 18 1,15 * *` | 매월 1일과 15일 오후 6시 30분 | 특정 날짜 지정 실행 |

---

## 3. crontab 필수 제어 명령어

```bash
# 1. 크론탭 작업 스케줄 편집 (최초 실행 시 나노/vi 에디터 선택)
crontab -e

# 2. 현재 등록된 크론탭 작업 목록 확인
crontab -l

# 🛑 [주의] 현재 등록된 모든 크론탭 작업 삭제 (실무에서 절대 주의!)
crontab -r
```

> ⚠️ **`crontab -r` 대참사 방지 팁:**
> `-e`(편집) 키 바로 옆에 `-r`(전체 삭제) 키가 붙어 있어서 실수로 잘못 치면 **등록된 스케줄이 경고 없이 싹 날아갑니다.** 스케줄을 수정하기 전에는 항상 `crontab -l > mycron.bak`처럼 백업본을 받아두는 습관이 안전합니다.

---

## 4. 🛑 실무에서 100% 터지는 crontab 4대 함정 (필독!)

터미널에서 직접 실행할 때는 잘 되던 스크립트가 **`crontab`에만 등록하면 동작하지 않는 경우**가 빈번하게 발생합니다. 이는 다음 4가지 원인 때문입니다.

### ① 모든 명령어 및 파일 경로에 '절대 경로' 사용
* **원인:** cron은 로그인 쉘과 달리 최소한의 환경변수(`PATH=/usr/bin:/bin`)만 가집니다.
* **해결:** `python3` 대신 `/usr/bin/python3`, `script.sh` 대신 `/home/ubuntu/script.sh`로 작성해야 합니다.

```bash
# ❌ 잘못된 예시 (상대 경로 및 환경변수 의존)
0 3 * * * python3 backup.py

# ⭕ 올바른 예시 (절대 경로 명시)
0 3 * * * /usr/bin/python3 /home/ubuntu/scripts/backup.py
```

### ② 실행 권한(`+x`) 누락
* 스크립트 파일(`.sh`)이 크론탭에 의해 실행되려면 해당 파일에 실행 권한이 부여되어 있어야 합니다.
```bash
chmod +x /home/ubuntu/scripts/backup.sh
```

### ③ 출력 및 에러 로그 리다이렉션 필수 지정
* cron은 기본적으로 실행 결과를 표준 출력(화면)으로 보여주지 못하므로, **에러나 실행 결과가 파일로 저장되도록 로그를 남겨야** 추적이 가능합니다.

```bash
# 실행 출력(1)과 에러(2)를 모두 cron.log 파일에 누적 기록(>>)
0 3 * * * /home/ubuntu/backup.sh >> /home/ubuntu/logs/cron.log 2>&1
```

### ④ 환경변수가 필요한 경우 스크립트 내부에서 로드
* Docker, Node.js, 특정 Python virtualenv 등 환경변수가 필수인 프로그램은 `.sh` 스크립트 상단에 `source /etc/profile` 또는 `source ~/.bashrc`를 작성해 주어야 합니다.

---

## 5. crontab 작업 실습 예시 (`.sh` 스크립트 연동)

### Step 1: 자동 실행할 쉘 스크립트 작성
```bash
vi /home/ubuntu/auto_check.sh
```

```bash
#!/bin/bash
echo "=== [$(date)] 시스템 상태 점검 시작 ===" >> /home/ubuntu/check.log
df -h | grep '/$' >> /home/ubuntu/check.log
```

### Step 2: 스크립트 실행 권한 부여
```bash
chmod +x /home/ubuntu/auto_check.sh
```

### Step 3: crontab에 10분마다 실행되도록 등록
```bash
crontab -e
```
```ini
# 매 10분마다 auto_check.sh 실행 후 모든 출력을 check.log에 누적
*/10 * * * * /home/ubuntu/auto_check.sh >> /home/ubuntu/check.log 2>&1
```

---

## 6. crontab 핵심 요약표

| 구분 | 명령어 / 표현 | 설명 |
| :--- | :--- | :--- |
| **목록 확인** | `crontab -l` | 현재 계정에 설정된 반복 스케줄 출력 |
| **스케줄 편집** | `crontab -e` | 크론탭 파일 수정 및 신규 스케줄 추가 |
| **전체 삭제** | `crontab -r` | **[주의]** 등록된 모든 스케줄 즉시 삭제 |
| **주기 설정** | `분 시 일 월 요일` | `*` (매 시각), `*/N` (N 간격), `,` (복수 지정) |
| **로그 저장** | `>> app.log 2>&1` | 표준 출력과 에러 메시지를 모두 파일에 누적 저장 |
| **핵심 규칙** | **절대 경로 사용** | 모든 실행 명령어 및 파일 경로는 전체 경로 기재 필수 |
