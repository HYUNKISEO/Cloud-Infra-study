# 04. 리눅스 보안의 꽃: 파일 권한과 소유자 설정 (File Permissions & Ownership)

> **학습일:** Day 4 <br>
> **학습 목표:** 리눅스의 다중 사용자 시스템 구조를 이해하고, `ls -l`로 출력되는 권한 표기법(rwx)을 해석하며, `chmod`와 `chown` 명령어를 사용해 파일과 디렉터리의 접근 권한 및 소유권을 안전하게 관리하는 기술을 익힙니다.

---

## 1. 파일 권한 구조와 `ls -l` 해석법

리눅스에서는 모든 파일과 디렉터리에 **접근 주체(소유자/그룹/기타)**별로 **읽기/쓰기/실행** 권한이 엄격하게 분리되어 있습니다.

```bash
# 1. 파일 상세 정보 확인
ls -l app.sh
# 출력 예시: -rwxr-xr-- 1 user01 devgroup 4096 Aug 18 10:00 app.sh
```

### 🔍 권한 문자열 10자리 구조 분석 (`-rwxr-xr--`)

| 구분 | 위치 | 예시 | 설명 |
| :--- | :---: | :---: | :--- |
| **파일 유형** | 1번째 | `-` | `-`: 일반 파일, `d`: 디렉터리, `l`: 심볼릭 링크 |
| **소유자 (User)** | 2~4번째 | `rwx` | 파일 소유자(u)의 권한 (읽기, 쓰기, 실행 가능) |
| **소유 그룹 (Group)** | 5~7번째 | `r-x` | 파일 소유 그룹(g)의 권한 (읽기, 실행 가능 / 쓰기 불가능) |
| **기타 사용자 (Others)** | 8~10번째 | `r--` | 나머지 모든 사용자(o)의 권한 (읽기만 가능) |

---

## 2. 권한의 종류와 8진수(Octal) 및 심볼릭 원리

권한은 **r(Read)**, **w(Write)**, **x(Execute)** 3가지로 구성되며, 이들의 조합을 8진수 숫자값 또는 심볼릭 기호로 표현합니다.

### 💡 rwx 권한별 숫자 점수 (2진수 연동)

| 권한 문자 | 의미 | 수치 (2진수 ➡️ 8진수) | 파일에서의 역할 | 디렉터리에서의 역할 |
| :---: | :--- | :---: | :--- | :--- |
| **`r`** | Read (읽기) | **4** ($2^2$, `100`) | 파일 내용 조회 (`cat`, `less`) | 디렉터리 내 목록 조회 (`ls`) |
| **`w`** | Write (쓰기) | **2** ($2^1$, `010`) | 파일 내용 수정/삭제 | 디렉터리 내 파일 생성/삭제 |
| **`x`** | Execute (실행) | **1** ($2^0$, `001`) | 스크립트/프로그램 실행 (`./app.sh`) | 디렉터리 내부 진입 (`cd`) |

#### 🔢 8진수 권한 합산 예시 (0~7)
* `rwx` = $4 + 2 + 1 =$ **`7`** (모든 권한 허용)
* `rw-` = $4 + 2 + 0 =$ **`6`** (읽기 + 쓰기)
* `r-x` = $4 + 0 + 1 =$ **`5`** (읽기 + 실행)
* `r--` = $4 + 0 + 0 =$ **`4`** (읽기 전용)
* `---` = $0 + 0 + 0 =$ **`0`** (권한 없음)

---

## 3. 파일/디렉터리 권한 변경 (`chmod`)

`chmod`(Change Mode)는 파일이나 디렉터리의 접근 권한을 변경할 때 사용합니다. 실무에서는 **8진수 표기법**과 **심볼릭 표기법** 두 가지 방식이 상황에 맞게 활용됩니다.

* **기본 구문:** `chmod [옵션] [권한] [파일명/디렉터리명]`

### ① 8진수 표기법 (Numeric Mode)
소유자(u), 그룹(g), 기타 사용자(o) 순서대로 숫자를 3자리로 한 번에 지정합니다. 전체 권한 상태를 고정해야 하는 서버 배포나 스크립트에서 필수적입니다.

```bash
# 1. 일반적인 실행 파일 권한 설정 (소유자: rwx, 그룹: r-x, 기타: r-x)
chmod 755 deploy.sh

# 2. 일반 문서 파일 권한 설정 (소유자: rw-, 그룹: r--, 기타: r--)
chmod 644 document.txt

# 3. 보안 파일/비밀키 권한 설정 (소유자만 rw-, 나머지는 접근 불가)
chmod 600 id_rsa

# 4. 디렉터리와 그 하위의 모든 파일/폴더 권한을 일괄 변경 (-R 옵션)
chmod -R 755 project_dir/
```

### ② 심볼릭 표기법 (Symbolic Mode)
기호(문자)와 연산자를 조합하여 기존 권한을 유지한 채 **특정 권한만 부분 수정**할 때 사용합니다.

* **대상:** `u` (User/소유자), `g` (Group), `o` (Others/기타), `a` (All/전체)
* **연산자:** `+` (권한 추가), `-` (권한 제거), `=` (권한 지정)
* **다중 설정:** 콤마(`,`)를 사용하면 서로 다른 대상의 권한을 한 번에 연달아 변경할 수 있습니다. (⚠️ 콤마 전후로 **공백 없이** 작성)

```bash
# 1. 소유자(u)에게 실행(x) 권한 추가
chmod u+x script.sh

# 2. 기타 사용자(o)에게서 쓰기(w) 및 실행(x) 권한 제거
chmod o-wx confidential.log

# 3. 그룹(g)과 기타 사용자(o)에게 읽기(r) 권한만 부여
chmod go=r public.txt

# 4. 콤마(,)를 사용해 복수의 대상 권한을 한 번에 변경 (공백 금지)
chmod u+x,o-w deploy.sh

# 5. 모든 사용자(a)에게 실행(x) 권한 추가 (chmod +x와 동일)
chmod a+x run.sh
```

---

## 4. 소유자와 소유 그룹 변경 (`chown`)

`chown`(Change Owner)은 파일이나 디렉터리의 **소유자(User)** 및 **소유 그룹(Group)**을 변경하는 명령어입니다. (주로 `root` 관리자 권한으로 실행)

* **기본 구문:** `chown [옵션] [소유자]:[소유그룹] [파일명/디렉터리명]`

```bash
# 1. 파일의 소유자만 변경
chown user02 app.py

# 2. 파일의 소유자와 소유 그룹을 동시에 변경
chown user02:devgroup app.py

# 3. 파일의 소유 그룹만 변경 (콜론 앞에 소유자 생략)
chown :devgroup app.py

# 4. 디렉터리와 내부 모든 파일/폴더의 소유권을 재귀적 일괄 변경 (-R 옵션)
chown -R www-data:www-data /var/www/html
```

> 💡 **참고:** 소유 그룹만 전용으로 변경할 때는 `chgrp devgroup app.py` 명령어를 사용할 수도 있습니다.

---

## 5. 디렉터리 권한의 특수성과 실무 주의사항

파일에서의 권한 동작과 디렉터리에서의 권한 동작은 의미가 다릅니다. 이 차이를 이해하지 못하면 실무에서 권한 에러를 겪게 됩니다.

```
┌─────────────────────────────────────────────────────────┐
│ ⚠️ 디렉터리 권한의 핵심 규칙                             │
│                                                         │
│ 1. 디렉터리에 'x'(실행) 권한이 없으면 'cd'로 진입 불가능!   │
│ 2. 디렉터리에 'w'(쓰기) 권한이 있으면, 내부 파일의         │
│    읽기 전용 상태와 상관없이 파일을 삭제할 수 있음!      │
└─────────────────────────────────────────────────────────┘
```

* **`r`만 있고 `x`가 없는 디렉터리:** `ls`로 내부 파일 이름 목록은 보이지만, 파일 상세 정보 조회 및 접근은 불가능합니다.
* **`x`만 있고 `r`이 없는 디렉터리:** `cd`로 진입하고 내부 파일명을 정확히 알면 접근할 수 있지만, `ls`로 목록 확인은 불가능합니다.

---

## 6. 실무 대표 권한 조합 & 보안 가이드

실무 서버 운영 시 자주 사용되는 대표적인 안전 권한 설정 조합입니다.

| 권한 (숫자) | 권한 (문자) | 대표적 사용 대상 | 보안 레벨 |
| :---: | :---: | :--- | :---: |
| **`644`** | `rw-r--r--` | 일반 웹 서버 파일 (`.html`, `.css`, `.jpg`) | 표준 |
| **`755`** | `rwxr-xr-x` | 일반 디렉터리, 실행 가능한 쉘 스크립트 (`.sh`) | 표준 |
| **`600`** | `rw-------` | SSH 개인키 (`id_rsa`), DB 패스워드 설정 파일 | 높음 (보안) |
| **`700`** | `rwx------` | 사용자 개인 홈 디렉터리, root 전용 디렉터리 | 높음 (보안) |

> 🛑 **보안 절대 경고 (`chmod 777` 사용 금지):**
> 권한 에러(`Permission denied`)가 난다고 `chmod 777`을 남발하면, 시스템의 모든 사용자가 파일 수정 및 악성 코드 침투를 할 수 있는 치명적인 보안 구멍이 됩니다. **최소 권한의 원칙(Principle of Least Privilege)**에 따라 필요한 권한만 최소한으로 부여하세요!

---

## 7. Day 4 핵심 명령어 & 권한 요약

| 구분 | 명령어 / 기호 | 주요 기능 | 실무 활용 예시 |
| :--- | :--- | :--- | :--- |
| **권한 변경** | **`chmod`** | 파일/디렉터리 접근 권한 변경 | `chmod 755 script.sh` / `chmod u+x,o-w run.sh` |
| **소유권 변경** | **`chown`** | 소유자 및 소유 그룹 변경 | `chown -R nginx:nginx /var/www` |
| **그룹 전용 변경** | **`chgrp`** | 소유 그룹만 전용으로 변경 | `chgrp devgroup app.py` |
| **재귀 옵션** | **`-R`** | 하위 모든 파일/폴더 일괄 적용 | `chmod -R 600 ~/.ssh` |
| **권한 점수** | **`r = 4`, `w = 2`, `x = 1`** | 읽기, 쓰기, 실행 점수 합산 | `7(rwx)`, `6(rw-)`, `5(r-x)`, `4(r--)` |

> 💡 **[심화 참고] 그룹을 2개 이상 지정하고 싶을 땐? (ACL 기능)** <br>
> 리눅스의 기본 구조는 **1개 파일당 소유자 1명 + 소유 그룹 1개**가 원칙입니다. 하지만 하나의 파일에 **"소유자 1명 + 개발팀(그룹1) + 디자인팀(그룹2)"**처럼 여러 그룹에게 각기 다른 권한을 줘야 하는 실무 상황이 생깁니다.<br>
> 이때는 확장 권한 기능인 **ACL(`setfacl`)**을 사용합니다.<br>
> * `setfacl -m g:devgroup:rw- file.txt` ➡️ 개발팀 그룹에는 읽기/쓰기(`rw-`) 권한 부여<br>
> * `setfacl -m g:designgroup:r-- file.txt` ➡️ 디자인팀 그룹에는 읽기 전용(`r--`) 권한 부여
