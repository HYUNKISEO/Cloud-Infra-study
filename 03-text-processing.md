# 03. 파일 내용 확인 및 실시간 검색 (Text Processing & Inspection)

> **학습일:** Day 3 <br>
> **학습 목표:** 파일의 전체 또는 일부 내용을 확인하고, 대용량 로그를 효율적으로 탐색하며, `grep`과 리다이렉션/파이프를 활용해 원하는 데이터를 실시간으로 검색·추출하는 기술을 익힙니다.

---

## 1. 파일 전체 내용 출력 (`cat`)

`cat`(Concatenate)은 파일의 전체 내용을 터미널 화면에 즉시 출력하거나 여러 파일을 하나로 연결할 때 사용합니다.

* **기본 구문:** `cat [옵션] [파일명]`
* **주요 옵션:**
  * `-n` (number): 모든 행(Line) 앞에 번호를 붙여서 출력합니다.
  * `-b` (number-nonblank): 비어 있지 않은 행에만 번호를 붙입니다.

```bash
# 1. 단일 파일의 전체 내용 출력
cat server.log

# 2. 행 번호(Line Number)를 포함하여 출력
cat -n server.log

# 3. 여러 파일의 내용을 순서대로 이어 붙여 출력
cat header.txt body.txt footer.txt
```

> ⚠️ **주의:** 파일 크기가 몇 백 MB 이상으로 매우 큰 경우, `cat`을 사용하면 터미널이 한동안 먹통이 되거나 수천 줄이 순식간에 지나가 버릴 수 있습니다. 대용량 파일은 아래의 `less`나 `head`/`tail`을 사용하는 것이 좋습니다.

---

## 2. 파일의 앞/뒷부분 조회 (`head`, `tail`)

파일 전체를 볼 필요 없이, 상단 또는 하단의 일부 내용만 빠르게 확인하고 싶을 때 사용합니다.

* **기본 구문:**
  * `head [옵션] [파일명]`
  * `tail [옵션] [파일명]`
* **주요 옵션:**
  * `-n [줄수]`: 출력할 행의 개수를 지정합니다. (기본값: 10줄)
  * `-f` (follow): **`tail` 전용 필수 옵션**. 파일에 새로 추가되는 내용을 실시간으로 모니터링합니다.

```bash
# 1. 파일의 상단 10줄 출력 (기본 동작)
head app.log

# 2. 파일의 상단 20줄 출력
head -n 20 app.log

# 3. 파일의 하단 10줄 출력 (기본 동작)
tail app.log

# 4. 파일의 하단 50줄 출력
tail -n 50 app.log

# 5. 실시간 로그 모니터링 (Ctrl + C로 종료)
tail -f server.log
```

---

## 3. 대용량 파일 페이지 단위 조회 (`less`)

`less`는 대용량 텍스트 파일도 메모리를 대량으로 소비하지 않고 빠른 속도로 훑어보거나 검색할 수 있는 터미널 뷰어입니다.

* **기본 구문:** `less [파일명]`

```bash
# 1. 대용량 로그 파일 조용하게 열기
less large_system.log
```

> 💡 `less` 화면 내부에서의 이동, 검색, 종료 관련 조작 단축키는 **7번 단축키 섹션**에 정리되어 있습니다.

---

## 4. 특정 문자열 검색 (`grep`)

`grep`(Global Regular Expression Print)은 파일 내에서 특정 패턴이나 단어를 포함한 행을 찾아 출력해 주는 리눅스 필수 검색 명령어입니다.

* **기본 구문:** `grep [옵션] [검색어] [파일명]`
* **주요 옵션:**
  * `-i` (ignore-case): 대소문자를 구분하지 않고 검색합니다.
  * `-n` (line-number): 일치하는 행의 번호를 함께 출력합니다.
  * `-v` (invert-match): 검색어가 **포함되지 않은** 행만 출력합니다.
  * `-r` (recursive): 하위 디렉터리의 모든 파일까지 순회하며 검색합니다.
  * `-c` (count): 매칭된 행의 총 개수만 출력합니다.

```bash
# 1. server.log에서 "ERROR" 문자열이 포함된 모든 행 검색
grep "ERROR" server.log

# 2. 대소문자 구분 없이 "error" 검색 및 행 번호 표시
grep -in "error" server.log

# 3. "INFO" 레벨을 제외한 나머지 로그만 보기
grep -v "INFO" server.log

# 4. src/ 디렉터리 내의 모든 파일에서 "TODO" 주석 검색
grep -rn "TODO" src/
```

---

## 5. 출력 연결과 저장 (`|`, `>`, `>>`)

명령어의 실행 결과를 다른 명령어의 입력으로 넘기거나, 파일로 저장할 때 **파이프(Pipe)**와 **리다이렉션(Redirection)**을 사용합니다.

```bash
# 1. 리다이렉션 (>): 결과를 파일로 저장 (기존 파일 내용 덮어쓰기)
cat header.txt body.txt > combined.txt

# 2. 리다이렉션 (>>): 결과를 파일 끝에 덧붙이기 (Append)
echo "LOG END" >> system.log

# 3. 파이프 (|): 앞 명령어의 출력을 뒤 명령어의 입력으로 전달
cat server.log | grep "ERROR"

# 4. 파이프 + less 조합: 대용량 검색 결과를 페이지 단위로 탐색
grep -i "exception" application.log | less
```

---

## 6. Day 3 핵심 명령어 요약

| 명령어 | 주요 기능 | 필수/핵심 옵션 | 비고 및 주의사항 |
| :--- | :--- | :---: | :--- |
| **`cat`** | 전체 파일 내용 출력 | `-n` | 대용량 파일에 사용 시 터미널 멈춤 주의 |
| **`head`** | 파일 상단 일부 출력 | `-n` | 기본값 10줄 출력 |
| **`tail`** | 파일 하단 일부 출력 | `-n`, `-f` | 실시간 로그 모니터링 시 `-f` 필수 사용 |
| **`less`** | 대용량 파일 가독 검색 | - | `q` 키로 종료, `/` 키로 내부 검색 |
| **`grep`** | 문자열 패턴 검색 | `-i`, `-n`, `-v`, `-r` | 리눅스 실무에서 가장 빈번히 쓰이는 검색 도구 |

---

## 7. Day 3 실무 핵심 단축키 & CLI 팁

| 구분 | 단축키 / 기호 | 주요 기능 | 활용 예시 |
| :--- | :---: | :--- | :--- |
| **CLI 조작** | **`Ctrl` + `C`** | 실행 중인 프로세스 강제 종료 | `tail -f` 모니터링 종료 시 사용 |
| **CLI 연동** | **`\|` (Pipe)** | 앞 명령어의 출력을 뒤 명령어의 입력으로 전달 | `cat server.log \| grep "404"` |
| **CLI 연동** | **`>` / `>>`** | 파일 저장 (덮어쓰기 / 덧붙이기) | `grep "ERROR" app.log > errors.txt` |
| **less 내 이동** | **`Space` / `PageDown`** | 한 페이지 아래로 이동 | - |
| **less 내 이동** | **`b` / `PageUp`** | 한 페이지 위로 이동 | - |
| **less 내 이동** | **`↓` / `j`** , **`↑` / `k`** | 한 줄 아래 / 위로 이동 | - |
| **less 내 이동** | **`G` (대문자) / `g` (소문자)** | 파일의 최하단(끝) / 최상단(처음)으로 이동 | 대용량 로그의 최신 부분 확인 시 `G` 사용 |
| **less 내 검색** | **`/검색어`** | 아래 방향으로 단어 검색 | `n`: 다음 결과, `N`: 이전 결과 |
| **less 종료** | **`q`** | `less` 뷰어 종료 후 터미널 복귀 | - |
