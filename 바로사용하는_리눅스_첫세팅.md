# 🚀 실전! 리눅스 초기 환경 자동 세팅 (Dotfiles)

새 깡통 리눅스 서버(AWS, Ubuntu, 가상머신 등)에 접속했을 때 단 한 줄의 명령어(One-Liner)로 나만의 단축어(`alias`)와 자동화 세팅을 구축하는 통합 매뉴얼입니다.

---

## 1. 깃허브 사전 준비 (저장소: `my-dotfiles`)

깃허브에 **`my-dotfiles`** 이름으로 저장소(Repository)를 생성하고, 아래 **2개 파일**을 올려둡니다.

### ① `.bash_aliases` (내 전용 단축어 모음집)
```bash
# 자주 쓰는 리눅스 명령어 단축어
alias d='echo 떡볶이'
alias ll='ls -al'
alias c='clear'

# 자주 쓰는 Git 명령어 단축어
alias gs='git status'
alias gp='git pull'
```

### ② `setup.sh` (원터치 자동 세팅 스크립트)
```bash
#!/bin/bash
GITHUB_ID="내아이디"

echo "🚀 리눅스 초기 환경 세팅을 시작합니다..."

# 1. my-dotfiles 저장소가 없으면 git clone 실행
if [ ! -d "$HOME/my-dotfiles" ]; then
    git clone [https://github.com/$GITHUB_ID/my-dotfiles.git](https://github.com/$GITHUB_ID/my-dotfiles.git) ~/my-dotfiles
fi

# 2. .bashrc 수첩에 중복 추가 방지 처리 후 연결 및 자동 git pull 등록
if ! grep -q "source ~/my-dotfiles/.bash_aliases" ~/.bashrc; then
    echo "source ~/my-dotfiles/.bash_aliases" >> ~/.bashrc
    echo "(cd ~/my-dotfiles && git pull --quiet)" >> ~/.bashrc
fi

echo "✅ 모든 세팅이 완료되었습니다!"
```

---

## 2. 새 리눅스 서버에서 실행 방법

### 💡 한 줄 실행법 (One-Liner - 가장 추천)
새 깡통 리눅스 서버에 접속하자마자 아래 한 줄만 복사해서 실행합니다.  
*(※ `내아이디` 부분을 본인 깃허브 ID로 변경하세요)*

```bash
curl -sL [https://raw.githubusercontent.com/내아이디/my-dotfiles/main/setup.sh](https://raw.githubusercontent.com/내아이디/my-dotfiles/main/setup.sh) | bash && source ~/.bashrc
```
# 1. 실행 권한 부여 및 실행
chmod +x setup.sh
./setup.sh

# 2. 변경사항 즉시 적용
source ~/.bashrc
```

---

## 3. 핵심 동작 원리 & 주의사항

1. **설정 순서의 중요성**:
   * 저장소 폴더가 없는데 `.bashrc` 설정만 먼저 하면 경로 에러가 발생하므로, 반드시 `git clone`으로 폴더를 내려받은 뒤 `.bashrc`에 연결해야 합니다.
2. **경로 표기 주의 (`~/.bashrc`)**:
   * `/.bashrc`는 리눅스 최상위 루트 디렉터리를 가리켜 접근 오류가 납니다. 반드시 내 계정의 홈 디렉터리를 뜻하는 **`~/.bashrc`** 경로를 사용합니다.
3. **자동 갱신 및 무한 동기화**:
   * `~/.bashrc` 파일에 `(cd ~/my-dotfiles && git pull --quiet)` 구문이 적혀 있어, **터미널을 켤 때마다 0.01초 만에 깃허브에서 최신 단축어를 자동으로 땡겨옵니다.**
4. **단축어 수정 및 유지보수**:
   * 노트북이나 집 컴퓨터에서 깃허브의 `.bash_aliases` 파일만 새로 수정/업로드(`git push`)해 두면, 서버 접속 시 자동으로 최신 상태로 업데이트됩니다.
