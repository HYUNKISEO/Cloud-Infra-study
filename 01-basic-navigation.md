# 01. 리눅스란 무엇인가 & 기본 파일 탐색 (`pwd`, `ls`, `cd`)

> **학습일:** Day 1  
> **목표:** 리눅스 OS의 기본 개념 및 디렉터리 구조를 이해하고, 커맨드 라인 인터페이스(CLI)에서 파일 시스템을 자유롭게 탐색할 수 있는 기초 명령어(`pwd`, `ls`, `cd`)를 습득한다.

---

## 1. 리눅스(Linux)란 무엇인가?

 ### 1.1 리눅스의 개요
  리눅스는 1991년 리누스 토르발스(Linus Torvalds)가 개발한 오픈소스 운영체제(OS)입니다. 
 윈도우(Windows)나 맥OS(macOS)처럼 사용자와 컴퓨터 하드웨어 사이를 중계해 주는 역할을 하지만, **운영체제의 핵심인 '커널(Kernel)'이 공개**되어 있어 누구나 자유롭게 수정하고 재배포할 수 있습니다.

 ### 1.2 서버 및 클라우드 환경에서 리눅스를 쓰는 이유
 * **높은 안정성 및 성능:** GUI(그래픽 화면) 없이 CLI(텍스트 명령어) 환경으로 구동하여 불필요한 리소스 소비가 적고, 장시간 연속 구동에 유리합니다.
 * **오픈소스 & 비용 절감:** 라이선스 비용이 없어 대규모 인프라 구축 시 비용 효율적입니다.
 * **보안성 및 권한 제어:** 다중 사용자(Multi-user) 환경에 최적화된 엄격한 권한 관리 체계를 갖고 있습니다.
 * **클라우드 글로벌 표준:** AWS, GCP 등 퍼블릭 클라우드의 가상 서버(EC2 등) 및 Docker 컨테이너의 약 90% 이상이 리눅스 환경을 기반으로 작동합니다.

 ### 1.3 리눅스의 구성 요소: 커널(Kernel)과 셸(Shell)
 * **커널 (Kernel):** OS의 핵심부로, CPU·메모리·디스크 등 물리적 하드웨어를 직접 제어합니다.
 * **셸 (Shell):** 사용자가 입력한 명령어를 해석하여 커널에 전달하는 번역기입니다. (예: `bash`, `zsh`)

```text
[ 사용자 ]  ──(명령어 입력)──▶  [ 셸 (Bash) ]  ──(시스템 콜)──▶  [ 커널 ]  ──(제어)──▶  [ 하드웨어 ]
```

---

## 2. 리눅스 디렉터리 구조 (Directory Structure)

윈도우는 `C:\`, `D:\`와 같이 드라이브 단위로 최상위 경로가 나뉘지만, 리눅스는 단 하나의 최상위 디렉터리인 **루트(`/`)**를 중심으로 모든 파일과 폴더가 나무처럼 퍼져 나가는 트리(Tree) 구조를 가집니다.

```text
/ (루트 디렉터리)
├── bin         # 기본 실행 명령어들 (ls, cd, pwd 등)
├── etc         # 시스템 및 애플리케이션의 설정 파일 (가장 중요)
├── home        # 일반 사용자들의 개인 홈 디렉터리 (예: /home/user)
├── root        # 최고 관리자(root) 전용 홈 디렉터리
├── var         # 변동성이 있는 파일들 (로그 파일: /var/log, DB 데이터 등)
└── usr         # 일반 사용자용 프로그램 및 라이브러리가 설치되는 공간
```

---

## 3. 경로 표현 방식: 절대 경로 vs 상대 경로

리눅스에서 파일이나 폴더의 위치를 지정할 때는 두 가지 경로 표현법을 사용합니다.

 ### 3.1 절대 경로 (Absolute Path)
  * **기준:** 루트 디렉터리(`/`)부터 시작하는 전체 경로
  * **특징:** 현재 내 위치가 어디든 상관없이 **항상 동일한 위치**를 가리킵니다.
  * **예시:** `/var/log/syslog`, `/home/ubuntu/project`

 ### 3.2 상대 경로 (Relative Path)
  * **기준:** 현재 내가 위치해 있는 디렉터리(Current Working Directory)
  * **기호 정의:**
    * `.` : 현재 디렉터리
    * `..` : 상위(부모) 디렉터리
    * `~` : 로그인한 사용자의 홈 디렉터리
  * **특징:** 현재 위치에 따라 가리키는 대상이 달라지며, 경로를 짧게 입력할 수 있습니다.
  * **예시:** `./config.json` (현재 폴더의 config.json), `../downloads` (상위 폴더의 downloads)

---

## 4. 기본 파일 탐색 핵심 명령어

 ### 4.1 `pwd` (Print Working Directory)
  현재 내가 작업 중인 디렉터리의 **절대 경로**를 화면에 출력합니다.
 
  * **기본 사용법:**
    ```bash
    $ pwd
    /home/ubuntu
    ```
  * **활용 팁:** 셸 환경에서 현재 위치를 상실했을 때 항상 `pwd`를 실행해 내 위치를 확인합니다.
 
 ### 4.2 `ls` (List)
  현재 디렉터리 또는 지정한 디렉터리 내부에 있는 파일과 폴더 목록을 출력합니다.
  
  * **기본 사용법:**
    ```bash
    $ ls
    Desktop  Documents  Downloads  Music
    ```
 
  * **자주 사용하는 주요 옵션:**
 
  | 옵션 | 설명 | 비고 |
  | :--- | :--- | :--- |
  | `-a` | 숨김 파일/폴더를 포함한 모든 파일 출력 | 파일명이 `.`으로 시작하면 숨김 파일 |
  | `-l` | 파일의 상세 정보(권한, 소유자, 용량, 수정일 등)를 리스트 형태로 출력 | 실무에서 가장 많이 사용 |
  | `-h` | 파일 용량을 읽기 쉬운 단위(K, M, G)로 표시 | `-l`과 조합하여 사용 |
  | `-t` | 파일이 수정된 시간 순서(최신순)로 정렬 | - |
  | `-r` | 정렬 순서를 역순으로 뒤집음 | - |
 
  * **옵션 조합 실무 예시:**
    ```bash
    # 숨김 파일까지 포함하여 상세 정보를 보기 쉽게 출력
    $ ls -alh
    total 32K
    drwxr-xr-x 4 ubuntu ubuntu 4.0K Aug 12 14:00 .
    drwxr-xr-x 3 root   root   4.0K Aug 12 13:00 ..
    -rw-r--r-- 1 ubuntu ubuntu  220 Aug 12 13:00 .bashrc
    -rw-r--r-- 1 ubuntu ubuntu 1.5K Aug 12 13:30 app.py
    ```
 
 #### 💡 `ls -l` 출력 상세 분석
  ```text
  -rw-r--r--  1 ubuntu ubuntu 1536 Aug 12 13:30 app.py
  │└────┬───┘  │   │      │     │         │       └─ 파일/디렉터리 이름
  │     │      │   │      │     │         └─ 최종 수정 일시
  │     │      │   │      └─ 파일 크기 (Bytes)
  │     │      │   └─ 소유 그룹 (Group)
  │     │      └─ 하드링크 수
  │     └─ 권한 (Permission: 읽기r, 쓰기w, 실행x)
  └─ 파일 타입 (-: 일반파일, d: 디렉터리, l: 심볼릭링크)
  ```
 
 ### 4.3 `cd` (Change Directory)
  디렉터리를 이동할 때 사용하는 명령어입니다.
 
  * **기본 사용법:**
    ```bash
    $ cd [이동할_경로]
    ```
 
  * **자주 쓰는 패턴 모음:**
    ```bash
    # 1. 홈 디렉터리로 이동 (~ 로 이동)
    $ cd
    $ cd ~
  
    # 2. 절대 경로를 이용한 이동
    $ cd /var/log
 
    # 3. 상대 경로를 이용한 이동 (상위 폴더로 이동)
    $ cd ..
 
    # 4. 상위의 상위 폴더로 이동
    $ cd ../..
 
    # 5. 직전에 머물렀던 디렉터리로 즉시 복귀 (Undo 개념)
    $ cd -
  
    # 6. 최상위 루트 디렉터리로 이동
    $ cd /
    ```
 
 ---

## 5. 실무 생산성을 높이는 CLI 핵심 팁

 * **Tab 키 (자동 완성):**  
   파일명이나 디렉터리명의 일부만 입력하고 Tab 키를 누르면 자동으로 완성됩니다. 두 번 누르면 가능한 목록이 출력됩니다. 오타 방지와 속도 향상을 위해 필수적으로 사용합니다.
 
 * **대소문자 구분:**  
   리눅스는 `File.txt`와 `file.txt`를 전혀 다른 별개의 파일로 인식합니다.
 
 * **공백(Space)이 들어간 폴더/파일명 다루기:**  
   공백이 포함된 경우 역슬래시(`\`)를 이용하거나 큰따옴표로 감싸야 합니다.  
   예: `cd My\ Documents` 또는 `cd "My Documents"`
 
 * **화면 정리 (`clear` 또는 `Ctrl` + `L`):**  
   터미널이 지저분할 때 화면을 깨끗하게 정리합니다.
   
 
 * **이전 명령어 재사용 (위/아래 화살표 및 `history`):**  
   키보드의 위/아래 화살표 키로 이전에 사용한 명령어를 다시 불러올 수 있습니다.

## 6. 터미널 필수 단축키 & 복사·붙여넣기 가이드

터미널 환경에서는 `Ctrl` + `C`가 복사가 아닌 **'프로세스 강제 종료'**로 동작하기 때문에 일반 프로그램과 단축키 체계가 다릅니다.

---

 ### 6-1. 키보드 핵심 단축키
 
  | 구분 | 단축키 | 기능 설명 | 활용 목적 |
  | :--- | :--- | :--- | :--- |
  | **입력·완성** | `Tab` | 파일 / 디렉터리명 자동 완성 | 오타 방지 및 입력 속도 향상 |
  | | `Tab` + `Tab` | 입력 가능한 모든 목록 출력 | 파일명·명령어 자동 탐색 |
  | **화면·이동** | `Ctrl` + `L` | 터미널 화면 깨끗하게 정리 | `clear` 명령어와 동일 |
  | | `Ctrl` + `A` | 커서를 줄의 맨 앞으로 이동 | 긴 명령어의 앞부분 수정 |
  | | `Ctrl` + `E` | 커서를 줄의 맨 뒤로 이동 | 명령어 끝으로 커서 이동 |
  | | `Ctrl` + `U` | 커서 위치 기준 앞쪽 내용 전체 삭제 | 잘못 입력한 명령어 초기화 |
  | | `위 / 아래 화살표` | 이전 / 다음 실행했던 명령어 호출 | 명령어 재사용 (`history`) |
  | **제어** | `Ctrl` + `C` | 현재 실행 중인 명령어 **즉시 강제 종료** | 무한 루프나 멈춤 현상 해결 |
 
 ---
 
 ### 6-2. 터미널 복사(Copy) & 붙여넣기(Paste) 방법
 
  #### ⌨️ 키보드 단축키
  | 방식 | 복사 (Copy) | 붙여넣기 (Paste) | 주요 사용 환경 |
  | :--- | :--- | :--- | :--- |
  | **표준 터미널** | `Ctrl` + `Shift` + `C` | `Ctrl` + `Shift` + `V` | 리눅스 / 우분투 기본 터미널 |
  | **클래식 / Windows** | `Ctrl` + `Insert` | `Shift` + `Insert` | Windows Terminal, CMD, PuTTY, MobaXterm |
  | **macOS** | `Cmd` + `C` | `Cmd` + `V` | macOS Terminal, iTerm2 |
 
  #### 🖱️ 마우스 활용법
  * **드래그 & 우클릭 (Drag & Right-Click):**  
    마우스로 원하는 텍스트를 드래그(블록 지정)하면 **자동으로 복사**되며, 터미널 빈 공간에서 **마우스 우클릭**을 한 번 하면 즉시 붙여넣어집니다. *(Windows Terminal, WSL, PuTTY 등)*
  * **마우스 휠(중간 버튼) 클릭:**  
    텍스트를 선택한 뒤, 붙여넣을 위치에서 **마우스 휠 버튼**을 누르면 클립보드를 거치지 않고 바로 붙여넣어집니다. *(리눅스 데스크톱 환경)*

---

> ⚠️ **주의 (Warning):**  
> 터미널에서 텍스트를 복사하려고 습관적으로 `Ctrl` + `C`를 누르면 실행 중인 코드나 명령어가 **즉시 중단(SIGINT)**됩니다. 복사할 때는 반드시 `Ctrl` + `Shift` + `C` 또는 `Ctrl` + `Insert`를 사용해야 합니다.

> 💡 **[심화 참고] 윈도우의 '바로가기' 파일 만들기 (심볼릭 링크 `ln -s`)** <br>
> 복잡하고 깊은 폴더 경로로 매번 이동하기 힘들 때, 원하는 위치에 '바로가기' 링크를 만들어 간편하게 접근할 수 있습니다.<br>
> 웹 서버 무중단 배포(`current` -> `v1.0.0`)나 프로그램 버전 교체 시 실무에서 매우 자주 쓰입니다.<br>
> * `ln -s [원본_실제경로] [바로가기_이름]`<br>
> * 예시: `ln -s /var/log/nginx/access.log my_log.txt` ➡️ `my_log.txt`를 열면 실제 로그 파일이 열림
