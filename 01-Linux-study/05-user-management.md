# 05. 리눅스 사용자 및 그룹 계정 관리 (User & Group Management)

> **학습일:** Day 5 <br>
> **학습 목표:** 리눅스의 다중 사용자 시스템 구조를 이해하고, `useradd`, `passwd`, `usermod` 등으로 계정과 그룹을 생성·수정하며, `sudo` 및 `visudo`를 통해 안전하게 관리자 권한을 부여하는 실무 기술을 익힙니다.

---

## 1. 계정 및 그룹 구조와 권한 기초 (`whoami`, `id`, `sudo`)

리눅스는 다중 사용자(Multi-User) 시스템이므로 각 사용자는 독자적인 계정 ID(UID)와 그룹 ID(GID)를 부여받아 권한을 분리합니다.

* **기본 명령어:**
  * `whoami`: 현재 로그인된 계정 이름 출력
  * `id [계정명]`: 계정의 UID, GID 및 소속 그룹 목록 상세 조회
  * `sudo` (SuperUser DO): 일반 계정이 최고 관리자(`root`) 권한을 일시적으로 차용하여 명령어를 실행하는 도구

```bash
# 1. 현재 로그인된 계정 이름 확인
whoami

# 2. 현재 계정의 상세 ID 및 속한 그룹 목록 확인
id

# 3. [권한 거부 에러 예시] 일반 계정으로 권한이 없는 시스템 주요 파일 접근 시도
cat /etc/shadow
# 출력 에러: cat: /etc/shadow: Permission denied (허가 거부)

# 4. [sudo 활용 예시] sudo를 붙여 관리자 권한으로 시스템 파일 조회
sudo cat /etc/shadow
# [sudo] password for alex: (본인 비밀번호 입력 후 정상 출력)
```

> 💡 **왜 계정/그룹 생성·수정 시 `sudo`가 필수일까요?**
> * 계정과 그룹을 새로 만들거나 수정하는 작업(`useradd`, `groupadd`, `usermod` 등)은 시스템 핵심 설정 파일인 **`/etc/passwd`**, **`/etc/shadow`**, **`/etc/group`**을 직접 수정하는 작업입니다.
> * 이 파일들은 보안상 최고 관리자(`root`)만 쓰기 권한을 가지므로, **일반 계정이 계정이나 그룹을 생성·수정하려면 반드시 `sudo`를 붙여 관리자 권한을 차용**해야만 작동합니다.

> 💡 **`sudo`를 사용하는 이유 (왜 root로 직접 접속하지 않나요?)**
> 1. **시스템 파괴 방지:** 항상 `root` 상태로 있으면 오타 하나로 시스템 전체가 삭제될 수 있어, 평소엔 일반 계정으로 작업하다 필요한 순간만 권한을 차용합니다.
> 2. **보안 및 책임 추적:** `root` 비밀번호를 공유할 필요 없이 각자 **본인 비밀번호**를 사용하며, 누가 어떤 관리자 명령을 실행했는지 로그(`/var/log/auth.log`)에 투명하게 남습니다.

---

## 2. 사용자 계정 생성 및 관리 (`useradd`, `passwd`, `usermod`, `userdel`)

계정을 생성할 때는 필수 옵션과 함께 사용해야 하며, 시스템 파일(`/etc/passwd` 등)을 변경해야 하므로 앞서 배운 `sudo` 권한을 사용합니다.

* **주요 명령어:**
  * `useradd`: 새 계정 생성 (`-m` 홈 디렉터리 생성, `-s` 기본 쉘 지정)
  * `passwd`: 비밀번호 설정 및 계정 상태 관리 (대화형 입력)
  * `usermod`: 계정 속성 수정 (`-l` 로그인 이름 변경, `-aG` 보조 그룹 추가)
  * `userdel`: 계정 삭제 (`-r` 홈 디렉터리 및 메일함 포함 삭제)

```bash
# 1. 새 계정 생성 (시스템 파일 수정을 위해 sudo 사용 + /bin/bash 쉘 지정)
sudo useradd -m -s /bin/bash alex

# 2. 비밀번호 설정 (명령어 실행 후 대화형 프롬프트에 비밀번호 2회 입력)
sudo passwd alex
# New password: (타이핑해도 보안상 화면에 글자가 보이지 않음 - 정상)
# Retype new password:

# 3. 계정 로그인 이름 변경 (구문: usermod -l [새이름] [기존이름])
sudo usermod -l bob alex

# 4. 계정 삭제 (-r 옵션을 넣어야 /home/alex 디렉터리까지 깔끔하게 삭제됨)
sudo userdel -r bob
```

> 💡 **인자 순서 팁 (`usermod -l`):** `cp`나 `mv`와 달리 `usermod -l 새이름 기존이름` 순서인 이유는, **`-l` 옵션 뒤에 '새로 바꿀 값'**을 지정하고 맨 마지막에 **'작업 대상 계정'**을 적는 리눅스 옵션 문법 때문입니다.

---

## 3. 그룹 관리 및 관리자 권한 부여 (`groupadd`, `sudo`, `visudo`)

여러 사용자에게 동일한 권한을 부여할 때 그룹을 활용하며, 일반 계정이 안전하게 관리자 작업을 수행하도록 `sudo` 권한을 제어합니다.

* **주요 개념 및 명령어:**
  * `groupadd`: 새 그룹 생성 (`/etc/group` 파일 수정이 수반되므로 `sudo` 필요)
  * `usermod -aG`: 기존 계정을 관리자 그룹(`sudo` 또는 `wheel`)에 추가
  * `visudo`: `/etc/sudoers` 권한 설정 파일을 안전하게 수정하는 전용 편집기

```bash
# 1. 새 그룹(devteam) 생성
sudo groupadd devteam

# 2. 사용자(alex)를 sudo 관리자 그룹에 추가 (Ubuntu: sudo / CentOS: wheel)
sudo usermod -aG sudo alex

# 3. sudoers 설정 파일 편집 (특정 사용자/그룹의 sudo 세부 권한 제어)
sudo visudo
# 설정 예시: alex ALL=(ALL:ALL) NOPASSWD: ALL (비밀번호 입력 없이 sudo 사용 허용)
```

> 💡 **`su` vs `sudo` 차이점:**
> * `su -`: root의 비밀번호를 알고 있어야 하며, 완전히 root 계정으로 전환함 (보안상 위험).
> * `sudo`: **사용자 본인의 비밀번호**로 관리자 권한만 일시 차용하며, 수행한 작업이 로그에 남아 추적이 가능함.

> 🛑 **실무 주의사항 (`-aG` 옵션):** `usermod`에서 `-G`만 사용하면 기존에 속해 있던 모든 보조 그룹에서 탈퇴되고 지정한 그룹만 남게 됩니다. 반드시 **`-a` (append) 옵션을 함께 붙여 `-aG`로 실행**하세요!

---

## 4. 계정 정보 저장 파일 및 계정 전환 (`/etc/passwd`, `/etc/shadow`, `su`)

리눅스는 계정 관련 주요 정보를 핵심 텍스트 파일로 관리하며, 계정 전환 시 환경변수 로딩 방식에 주의해야 합니다.

* **주요 파일 및 개념:**
  * `/etc/passwd`: 전체 계정 목록 및 기본 설정 (UID 0: root, 1~999: 시스템 계정, 1000+: 일반 계정)
  * `/etc/shadow`: 암호화된 비밀번호 및 만료일 저장 (root만 읽기 가능)
  * `/etc/group`: 시스템 전체 그룹 목록 및 소속 사용자 정보
  * `/etc/skel`: 계정 생성 시 홈 디렉터리로 복사되는 초기 환경 파일 뼈대

```bash
# 1. 현재 시스템의 계정 정보 확인
cat /etc/passwd

# 2. 계정 전환 (기존 환경변수 유지 - 비권장)
su alex

# 3. 계정 전환 (홈 디렉터리 및 환경변수까지 완전히 전환 - 실무 권장)
su - alex
```

> 💡 **실무 팁 (`/etc/skel` 활용):** 새 계정이 생성될 때 모든 사용자에게 공통 단축어(`.bashrc`)나 기본 환경설정을 제공하고 싶다면 `/etc/skel/` 디렉터리 안에 해당 파일들을 미리 작성해두면 됩니다.

---

## 5. 실무 계정 보안 & 유지보수 (`passwd -l`, `chage`, `w`, `last`)

보안 강화를 위해 퇴사자 계정 잠금, 비밀번호 만료 주기 설정, 실시간 접속자 모니터링 조치를 수행합니다.

```bash
# 1. 계정 일시 잠금 (Lock - 퇴사자 발생 또는 보안 이슈 시 즉시 차단)
sudo passwd -l alex

# 2. 계정 잠금 해제 (Unlock)
sudo passwd -u alex

# 3. 비밀번호 90일 만료 주기 설정 및 정보 확인
sudo chage -M 90 alex
sudo chage -l alex

# 4. 현재 접속 중인 사용자 및 수행 작업 실시간 확인
w

# 5. 최근 접속/로그인 기록 조회 (보안 감사용)
last
```

> 🛑 **보안 절대 경고 (root 직접 로그인 금지):** SSH 설정 파일(`/etc/ssh/sshd_config`)에서 `PermitRootLogin no`로 설정하여 root 직접 접속을 반드시 차단하세요. 모든 관리는 일반 계정 접속 후 `sudo`를 사용하는 **최소 권한의 원칙**을 따라야 책임 소재 파악 및 오작동 방지가 가능합니다.

---

## 6. Day 5 핵심 명령어 & 옵션 통합 요약

| 명령어 | 주요 역할 | 필수/핵심 옵션 | 실무 활용 및 주의사항 |
| :--- | :--- | :---: | :--- |
| **`useradd`** | 사용자 계정 생성 | `-m`, `-s`, `-g`, `-G` | `-m`(홈 디렉터리), `-s`(기본 쉘) 지정 필수 / `sudo` 필요 |
| **`passwd`** | 비밀번호 설정 및 계정 잠금 | `-l`, `-u` | 입력 시 화면에 안 보임 / `-l`로 즉시 계정 차단 |
| **`usermod`** | 계정 속성 및 그룹 수정 | `-aG`, `-l`, `-s` | 그룹 추가 시 반드시 `-aG` 사용 / `-l 새이름 기존이름` |
| **`userdel`** | 사용자 계정 삭제 | `-r` | `-r` 미사용 시 홈 디렉터리가 남아 유령 파일 생성 |
| **`sudo`** | 관리자 권한 일시 실행 | - | 본인 비밀번호 사용 / 시스템 파일 수정 명령에 필수 |
| **`groupadd`**| 신규 그룹 생성 | - | 그룹 단위 권한 관리 시 사전 작성 / `sudo` 필요 |
| **`su`** | 사용자 계정 전환 | `-` (로그인 쉘) | 반드시 `su - 계정명`으로 완전 환경변수 전환 권장 |
| **`visudo`** | `/etc/sudoers` 안전 편집 | - | 문법 오류 검증을 제공하는 sudo 전용 안전 편집기 |

---

## 7. Day 5 실무 핵심 파일 & 계정 관리 CLI 팁

| 구분 | 주요 대상 | 역할 및 사용법 | 실무 활용 예시 |
| :--- | :---: | :--- | :--- |
| **핵심 파일** | **`/etc/passwd`** | 시스템 전체 사용자 목록 및 계정 정보 | UID 0(root), UID 1000+(일반 사용자) 구분 |
| **핵심 파일** | **`/etc/shadow`** | 암호화된 비밀번호 및 만료일 저장 | root 전용 보안 파일 (`sudo` 없이는 읽기 불가능) |
| **핵심 파일** | **`/etc/skel`** | 계정 생성 시 복사되는 초기 환경 뼈대 | 공통 `.bashrc` 환경 배포 시 내부 파일 수정 |
| **자동화 팁** | **`chpasswd`** | 대화형 입력 없는 비밀번호 일괄 설정 | `echo "alex:p@ss123" \| sudo chpasswd` |
| **모니터링** | **`w` / `last`** | 현재 접속자 / 과거 접속 이력 조회 | 서버 보안 점검 및 상시 모니터링 |

> 💡 **[심화 참고] 대화형 입력 없이 스크립트로 계정 자동화하기** <br>
> 서버 수십 대에 계정을 동시에 배포할 때는 대화형 `passwd` 대신 **`chpasswd`** 명령어를 사용합니다.
> * `echo "alex:MyPassword123!" | sudo chpasswd` ➡️ 대화창 입력 없이 한 줄로 비밀번호를 즉시 등록할 수 있어 자동화 스크립트 작성 시 필수입니다.
