# 07. 실행 중인 프로그램 모니터링 및 제어 (ps, top, kill)

> **학습일:** Day 7 <br>
> **학습 목표:** 리눅스 프로세스의 개념과 PID 구조를 이해하고, `ps`, `top`을 활용해 시스템 자원 점유 상태를 실시간으로 모니터링하며, `kill` 및 시그널(Signal)을 사용해 비정상 프로세스를 제어 및 종료하는 기술을 익힙니다.

---

## 1. 프로세스(Process)와 PID 개념 이해

디스크에 저장된 실행 가능한 파일이 **'프로그램(Program)'**이라면, 메모리에 로드되어 OS에 의해 실제 실행 중인 상태를 **'프로세스(Process)'**라고 합니다.

* **프로세스 식별자 및 핵심 개념:**
  * **PID (Process ID):** 실행 중인 각 프로세스에 할당되는 고유 번호 (1번 프로세스: systemd/init)
  * **PPID (Parent Process ID):** 해당 프로세스를 생성한 부모 프로세스의 PID
  * **포그라운드(Foreground):** 터미널 화면을 점유하며 사용자와 직접 주고받는 프로세스
  * **백그라운드(Background):** 터미널 화면 뒤에서 비가시적으로 동작하는 프로세스

> 💡 **왜 프로세스 제어 시 `sudo`가 필요할까요?**
> * 본인 계정이 실행한 프로세스는 일반 권한으로 종료(`kill`)할 수 있습니다.
> * 하지만 다른 계정이나 시스템 핵심 서비스(`root` 소유)를 모니터링하고 종료하려면 관리자 권한(`sudo`)이 필요합니다.

---

## 2. 프로세스 상태 조회 및 검색 (`ps`, `pgrep`)

현재 시스템에서 실행 중인 프로세스 목록과 메모리/CPU 점유율을 단발성 스냅샷으로 조회합니다.

* **주요 옵션 및 스타일:**
  * **BSD 스타일 (`ps aux`):** 가장 흔히 사용되는 스타일 (`a`: 타인 프로세스 포함, `u`: 사용자 정보 표시, `x`: 터미널 미할당 프로세스 포함)
  * **System V 스타일 (`ps -ef`):** 전체 프로세스를 표준 형태로 조회 (`-e`: 모든 프로세스, `-f`: 풀 포맷/PPID 포함)
  * **`pgrep`:** 프로세스 이름으로 PID만 빠르게 검색

```bash
# 1. BSD 스타일로 전체 프로세스 상세 조회 (CPU/MEM 점유율 확인 시 유용)
ps aux

# 2. 파이프(|)와 grep을 조합하여 특정 프로세스(예: nginx)만 검색
ps aux | grep nginx

# 3. System V 스타일로 조회 (부모 프로세스 ID인 PPID 확인 시 유용)
ps -ef | grep python

# 4. 특정 프로세스 이름의 PID만 직접 추출 (스크립트 작성 시 유용)
pgrep -fl nginx
```

> 💡 **`ps aux` 주요 출력 항목 보는 법:**
> * `USER`: 프로세스를 실행한 계정
> * `PID`: 프로세스 고유 번호
> * `%CPU` / `%MEM`: CPU 및 메모리 점유율
> * `STAT`: 프로세스 상태 (`R`: 실행 중, `S`: 대기 중, `Z`: 좀비 프로세스, `D`: 중단 불가능한 입출력 대기)

---

## 3. 실시간 프로세스 및 시스템 자원 모니터링 (`top`, `htop`)

터미널에서 CPU, 메모리 사용량, 로드 애버리지(Load Average)를 실시간(기본 3초 간격)으로 모니터링합니다.

* **`top` 실행 중 주요 단축키:**
  * `M`: 메모리 사용량 높은 순 정렬
  * `P`: CPU 사용량 높은 순 정렬 (기본값)
  * `1`: CPU 코어별 개별 점유율 보기/숨기기
  * `k`: PID를 직접 입력하여 프로세스 종료
  * `q`: top 모니터링 종료

```bash
# 1. 실시간 모니터링 실행
top

# 2. [실무 팁] 가시성이 훨씬 뛰어난 차세대 모니터링 도구 htop (별도 설치 필요)
sudo apt install -y htop  # Ubuntu
# sudo yum install -y htop # CentOS (EPEL 필요)
htop
```

> 🛑 **실무 주의사항 (Load Average):**
> * `top` 상단의 **Load Average**는 최근 1분, 5분, 15분 동안의 시스템 작업 대기열 수치입니다.
> * 이 값이 **CPU 코어 수**보다 지속적으로 높다면 서버 과부하 상태이므로 점유율이 높은 프로세스를 점검해야 합니다.

---

## 4. 프로세스 강제 종료 및 시그널 전달 (`kill`, `killall`, `pkill`)

비정상 동작을 하거나 자원을 과도하게 점유하는 프로세스에 시그널(Signal)을 보내 종료시킵니다.

* **주요 시그널 번호:**
  * **15 (`SIGTERM`):** 정상 종료 요청 (기본값, 프로세스가 작업 정리 후 종료됨)
  * **9 (`SIGKILL`):** 강제 종료 (OS가 프로세스를 즉시 강제 사살, 데이터 손실 가능성 존재)
  * **1 (`SIGHUP`):** 재설정/재로드 (프로세스 종료 없이 설정 파일 다시 읽기)
  * **2 (`SIGINT`):** 인터럽트 (키보드 `Ctrl + C`와 동일)

```bash
# 1. PID를 사용한 정상 종료 요청 (SIGTERM)
kill 1234

# 2. 응답 없는 프로세스 강제 종료 (SIGKILL -9)
sudo kill -9 1234

# 3. 프로세스 이름으로 일괄 강제 종료
sudo pkill -9 nginx
sudo killall -9 python3
```

> 🛑 **실무 절대 경고 (`kill -9` 사용 주의):**
> * `kill -9`는 데이터베이스나 파일 쓰기 작업을 수행 중인 프로세스의 경우 데이터 유실 및 DB 손상을 일으킬 수 있습니다. 실무에서는 먼저 `kill [PID]` (15번)로 안전하게 종료를 시도한 뒤, 응답이 없을 때 최후의 수단으로 `kill -9`를 사용하세요.
> * **`kill -9`로도 안 죽는 경우 (`D` 상태):** `ps aux`에서 `STAT`가 **`D`** (Uninterruptible Sleep)인 프로세스는 디스크/네트워크 입출력을 커널 수준에서 기다리는 중이어서 시그널을 전달받지 못합니다. 이 경우 I/O 작업이 끝나길 기다리거나 서버 재부팅이 필요할 수 있습니다.
> * **시스템 데몬 관리 정석:** Nginx, MySQL 등 시스템 서비스는 `kill`보다 `sudo systemctl stop 서비스명` 명령어로 제어하는 것이 정석입니다.

---

## 5. 백그라운드 작업 관리 및 터미널 세션 유지 (`&`, `jobs`, `fg`, `bg`, `nohup`)

명령어를 백그라운드로 실행하거나, SSH 접속이 끊겨도 프로세스가 계속 유지되도록 설정합니다.

* **주요 명령어 및 키 조합:**
  * `명령어 &`: 명령어 실행 시 맨 뒤에 `&`를 붙여 백그라운드에서 실행
  * `Ctrl + Z`: 현재 포그라운드 실행 중인 작업을 일시 중지(Stopped)
  * `jobs`: 현재 터미널 작업 목록 조회
  * `fg %[작업번호]`: 백그라운드 작업을 포그라운드로 전환
  * `bg %[작업번호]`: 일시 중지된 작업을 백그라운드 실행 상태로 전환
  * `nohup`: 터미널 세션이 종료(Hangup)되어도 백그라운드 프로세스를 계속 유지

```bash
# 1. 오랜 시간이 걸리는 작업을 백그라운드로 실행
python3 script.py &

# 2. 현재 터미널의 백그라운드 작업 목록 확인
jobs

# 3. 1번 작업을 다시 포그라운드로 가져오기
fg %1

# 4. SSH 세션이 끊겨도 로그를 남기며 무중단 실행 (nohup)
nohup python3 script.py &

# 5. [실무 팁] 지정한 로그 파일로 출력과 에러를 한 번에 저장하며 무중단 실행
nohup python3 app.py > app.log 2>&1 &

# 6. [실무 팁] 로그를 남기지 않고 완전히 무시하며 실행
nohup python3 app.py > /dev/null 2>&1 &

# 7. nohup 출력 로그 파일 실시간 모니터링
tail -f app.log
```

---

## 6. Day 7 핵심 명령어 & 옵션 통합 요약

| 명령어 | 주요 역할 | 필수/핵심 옵션 | 실무 활용 및 주의사항 |
| :--- | :--- | :---: | :--- |
| **`ps`** | 프로세스 스냅샷 조회 | `aux`, `-ef` | `ps aux \| grep [이름]` 형태로 프로세스 PID 검색 |
| **`top`** | 실시간 자원 모니터링 | - | `M`(메모리순), `P`(CPU순), `q`(종료) 단축키 활용 |
| **`pgrep`** | 프로세스 PID 검색 | `-f`, `-l` | 프로세스 이름에 해당하는 PID 목록만 빠르게 출력 |
| **`kill`** | 특정 PID 프로세스 종료 | `-9` (SIGKILL), `-15` (SIGTERM) | 기본 15번 시도 후 미응답 시 -9 강제 종료 |
| **`pkill`** | 프로세스 이름으로 종료 | `-9`, `-f` | PID를 몰라도 프로세스 이름으로 일괄 제어 |
| **`jobs`** | 백그라운드 작업 목록 | - | 현재 터미널 세션에 속한 작업 상태만 표시 |
| **`nohup`** | 터미널 종료 후 무중단 실행 | - | `nohup [명령어] > 파일 2>&1 &` 형태로 배포 자동화에 활용 |

---

## 7. Day 7 실무 핵심 프로세스 관리 CLI 팁

| 구분 | 주요 대상 | 역할 및 사용법 | 실무 활용 예시 |
| :--- | :---: | :--- | :--- |
| **상태 코드** | **`STAT` (ps 출력)** | `R`(Run), `S`(Sleep), `Z`(Zombie), `D`(Disk IO) | `Z`는 부모 점검, `D`는 디스크 병목 확인 |
| **로그 리다이렉션**| **`> app.log 2>&1`** | 표준 출력(1)과 표준 에러(2)를 동일 파일에 기록 | 백엔드 애플리케이션 백그라운드 실행 시 필수 |
| **프로세스 나무**| **`pstree`** | 부모-자식 프로세스 간의 계층 구조를 트리로 표시 | `pstree -p`로 PID 포함 전체 계층 확인 |
| **포트-PID 매핑**| **`lsof` / `ss`** | 특정 포트를 점유 중인 프로세스 PID 확인 | `sudo lsof -i :8080`으로 포트 충돌 원인 파악 |
