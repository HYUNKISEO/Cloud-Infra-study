# 02. 파일 및 디렉터리 제어 (File & Directory Control)

> **학습일:** Day 1  
> **학습 목표:** 리눅스 터미널에서 파일과 디렉터리를 생성, 복사, 이동, 이름 변경, 삭제하는 핵심 명령어를 익히고 실무 권장 습관을 체득합니다.

---

## 1. 디렉터리 생성 (`mkdir`)

`mkdir`(Make Directory)은 새로운 디렉터리(폴더)를 생성합니다.

* **기본 구문:** `mkdir [옵션] [디렉터리명]`
* **주요 옵션:**
  * `-p` (parents): 하위 경로까지 포함된 중첩 디렉터리를 생성할 때, 존재하지 않는 상위 디렉터리까지 한 번에 생성합니다.

```bash
# 1. 단일 디렉터리 생성
mkdir project

# 2. 여러 디렉터리 동시 생성
mkdir docs src bin

# 3. 중첩된 하위 디렉터리 구조 한 번에 생성 (project/src/components)
mkdir -p project/src/components
```

---

## 2. 빈 파일 생성 (`touch`)

`touch`는 0바이트 크기의 빈 파일을 생성하거나, 이미 존재하는 파일의 최종 수정 시간(Timestamp)을 현재 시간으로 업데이트합니다.

* **기본 구문:** `touch [파일명]`

```bash
# 1. 단일 빈 파일 생성
touch index.html

# 2. 여러 파일 동시 생성
touch app.js style.css README.md
```

---

## 3. 파일 및 디렉터리 복사 (`cp`)

`cp`(Copy)는 파일이나 디렉터리를 다른 위치로 복사합니다.

* **기본 구문:** `cp [옵션] [원본] [대상]`
* **주요 옵션:**
  * `-r` (recursive): **디렉터리를 복사할 때 필수**로 사용하며, 내부의 모든 하위 파일과 디렉터리를 함께 복사합니다.
  * `-i` (interactive): 대상 위치에 동일한 이름의 파일이 이미 존재할 경우 덮어쓸지 여부를 확인합니다.
  * `-v` (verbose): 복사 진행 과정을 상세하게 출력합니다.

```bash
# 1. 파일 복사 (app.js를 app_backup.js라는 이름으로 복사)
cp app.js app_backup.js

# 2. 파일을 특정 디렉터리 내부로 복사
cp index.html project/src/

# 3. 디렉터리 전체 복사 (src 폴더를 src_backup 폴더로 복사)
cp -r src src_backup
```

---

## 4. 파일 및 디렉터리 이동 & 이름 변경 (`mv`)

`mv`(Move)는 파일이나 디렉터리의 위치를 이동시키거나 **이름을 변경**할 때 사용합니다.  
*(리눅스에서는 '이름 변경'과 '이동'이 동일한 원리로 동작하며, 디렉터리를 이동할 때도 `-r` 같은 별도 옵션이 필요하지 않습니다.)*

* **기본 구문:** `mv [옵션] [원본] [대상]`

```bash
# 1. 이름 변경 (동일한 경로 내에서 지정)
mv style.css main.css

# 2. 파일 위치 이동
mv main.css project/src/

# 3. 이동과 동시에 이름 변경
mv README.md project/docs/READ_FIRST.md

# 4. 디렉터리 이름 변경
mv project my-project
```

---

## 5. 파일 및 디렉터리 삭제 (`rm`, `rmdir`)

`rm`(Remove)은 파일이나 디렉터리를 삭제하고, `rmdir`(Remove Directory)은 비어 있는 디렉터리를 삭제합니다.

* **기본 구문:**
  * `rm [옵션] [대상]`
  * `rmdir [디렉터리명]`
* **주요 옵션 (`rm`):**
  * `-r` (recursive): 디렉터리와 그 내부의 모든 하위 콘텐츠를 순회하며 삭제합니다.
  * `-f` (force): 삭제 확인 메시지나 경고를 출력하지 않고 강제로 영구 삭제합니다.
  * `-i` (interactive): 파일/디렉터리를 삭제할 때마다 정말 삭제할 것인지 물어봅니다.

```bash
# 1. 비어 있는 디렉터리 삭제 (내부에 파일이 있으면 오류 발생)
rmdir empty_folder

# 2. 단일 파일 및 여러 파일 삭제
rm app_backup.js
rm file1.txt file2.txt

# 3. 디렉터리 및 하위 콘텐츠 전체 삭제
rm -r src_backup

# 4. 디렉터리 강제 영구 삭제 (주의!)
rm -rf build
```

> 🛑 **실무 주의사항 & 안전 습관**
> 1. **휴지통 없는 영구 삭제:** 리눅스 CLI 환경의 삭제는 **휴지통(Trash)을 거치지 않고 즉시 파일 시스템에서 제거**되므로 일반적인 방법으로는 복구가 불가능합니다.
> 2. **`rm -rf` 실행 전 `pwd` 재확인:** 잘못된 경로에서 `rm -rf`를 실행하면 프로젝트 전체 또는 서버 운영 체제가 파괴될 수 있습니다. 삭제 명령 입력 직전에는 항상 **`pwd`로 현재 작업 디렉터리를 확인**하는 것이 실무 표준 수칙입니다.
> 3. **띄어쓰기 오타 치명상 예방:** `rm -rf / var/log`처럼 경로 사이에 실수로 공백(스페이스)을 넣으면, 최상위 루트(`/`) 전체를 삭제하는 명령으로 해석되어 서버가 즉시 다운될 수 있습니다.
> 4. **백업 및 `-i` 옵션 활용:** 중요한 데이터나 운영 서버 환경에서는 삭제 전 미리 복사본(`cp`)을 만들어 두거나, `-i` 옵션을 붙여 안전하게 삭제 대상을 하나씩 확인하는 습관이 좋습니다.

---

## 6. Day 2 핵심 명령어 요약

| 명령어 | 주요 기능 | 필수/핵심 옵션 | 비고 및 주의사항 |
| :--- | :--- | :---: | :--- |
| **`mkdir`** | 디렉터리 생성 | `-p` | 중첩 디렉터리 구조 한 번에 생성 가능 |
| **`touch`** | 빈 파일 생성 및 시간 갱신 | - | 파일이 존재하지 않을 때 0 Byte 빈 파일 생성 |
| **`cp`** | 파일 및 디렉터리 복사 | `-r` | 디렉터리 복사 시 `-r` 옵션 미사용 시 오류 발생 |
| **`mv`** | 파일/디렉터리 이동 및 이름 변경 | - | 동일 경로 지정 시 이름 변경, 다른 경로는 이동 |
| **`rm`** | 파일 및 디렉터리 영구 삭제 | `-r`, `-f` | 삭제 시 휴지통 없이 영구 삭제되므로 `pwd` 확인 필수 |
